<resources>

    <!-- Base application theme. NoActionBar para remover a toolbar padrão-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:popupTheme">@style/PopUpMenuTheme</item>
    </style>

<!--    estilo do botão elevado-->
    <style name="ElevadoButton" parent="TextAppearance.AppCompat.Widget.Button.Colored">
        <item name="android:backgroundTint">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

<!--    estilo do flat button botão plano-->
    <style name="PlanoButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="PopUpMenuTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@android:color/white</item>
    </style>




</resources>
